<div class="container">
        <h1>Editing Announcement</h1>

        <%= render 'form', announcement: @announcement %>
        <br>
        <div>
                <%= link_to 'Show', @announcement, class:"btn btn-info" %>
                <%= link_to 'Back', announcements_path, class:"btn btn-secondary" %>
        </div>

</div>