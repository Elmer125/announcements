<p style="color: green"><%= notice %></p>
<div class="container">
    <%= render @announcement %>
    <div>
        <% if current_user?(@announcement.user)%>
            <%= link_to "Edit", edit_announcement_path(@announcement) %> |
            <%= button_to "Destroy", @announcement, method: :delete %>
        <% end %>

        <%= link_to "Back", announcements_path %>
    </div>
</div>
